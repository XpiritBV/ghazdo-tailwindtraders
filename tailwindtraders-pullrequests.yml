trigger:
  branches:
    exclude:
      - "*"

pr:
  branches:
    include:
      - "*"

pool:
  vmImage: windows-latest

steps:
  - script: echo This pipeline runs for all pull requests
